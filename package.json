{
  "name": "aperitivi-urbani-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node scripts/validate-locali.mjs",
    "analytics": "node scripts/analytics-report.mjs",
    "check": "astro check",
    "test": "node --import tsx --test tests/*.test.mjs",
    "cms:seed": "node scripts/generate-cms-seed.mjs",
    "verify": "npm run validate && npm run test && npm run check && npm run build"
  },
  "dependencies": {
    "astro": "^7.1.4",
    "jose": "^6.2.4",
    "rehype-sanitize": "^6.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/cloudflare": "^14.1.5",
    "@astrojs/markdown-remark": "^7.2.1",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0",
    "tsx": "^4.23.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.114.0"
  }
}
